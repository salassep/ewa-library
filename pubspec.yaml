name: ewa
description: a library for securing data by embedding it in an audio using a combination of EAS and AES.
version: 1.1.1
repository: https://github.com/salassep/ewa-library.git

environment:
  sdk: ^3.0.1

dependencies: 
  path: ^1.8.3
  encrypt: ^5.0.1

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
